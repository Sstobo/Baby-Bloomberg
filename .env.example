# Frontend (create .env.local with these)
VITE_CONVEX_URL=           # Your Convex deployment URL (from `npx convex dev`)

# Backend (set via `npx convex env set KEY VALUE`)
# FINNHUB_API_KEY=         # Free at finnhub.io — real-time stock quotes & news
# FMP_API_KEY=             # Free at financialmodelingprep.com — financial statements
# FIRECRAWL_API_KEY=       # Free at firecrawl.dev — AI article/filing analysis
# GOOGLE_GENERATIVE_AI_API_KEY=  # Google AI Studio — powers Gemini analysis LLM
